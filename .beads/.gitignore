# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Generated JSONL files (ignore all except issues.jsonl)
beads.*.jsonl
beads.*.meta.json

# Keep only essential files (source of truth for git)
!issues.jsonl
!metadata.json
!config.yaml
